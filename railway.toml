[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build && npm -w backend/aero-gateway run build"

[deploy]
startCommand = "npm -w backend/aero-gateway run start"
healthcheckPath = "/healthz"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
